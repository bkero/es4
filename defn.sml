structure Defn = struct


(* 
 * I *think* we need the following here:
 * 
 *    - code to calculate any "compile-time constant expressions", in particular namespace values.
 *    - code to build the layout and type for every class, interface and function, and bind them 
 *      together into static scopes.
 *)    
    

end
